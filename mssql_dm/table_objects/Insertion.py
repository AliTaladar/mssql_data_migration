class Insertion:
    def __init__(self, schema, table_name, columns, values):
        self.schema = schema
        self.table_name = table_name
        self.columns = columns
        self.values = values